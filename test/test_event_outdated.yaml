TestEventOutdated:
    sources:
        - BasketballEvents:
            class: monitor.source.peerplays_events.PeerplaysEvents

    triggers:
        - OutdatedEventTrigger:
            class: monitor.trigger.event_outdated.EventOutdated
            time_window: 300
            #downtime: 100  # --> 100 seconds no trigger
            level: 2

    actions:
        - action:
            class: monitor.action.stdout.Stdout
            level: 2
